import styled from 'styled-components';
import { Typography } from '@material-ui/core';

const CommentUser = styled(Typography)`
  font-weight: 400;
  font-size: 14px;
  line-height: 18px;
  margin-right: 10px;
`;

export default CommentUser;
