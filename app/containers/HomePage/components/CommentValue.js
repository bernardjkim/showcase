import styled from 'styled-components';
import { Typography } from '@material-ui/core';

const CommentValue = styled(Typography)`
  font-weight: 400;
  font-size: 16px;
  line-height: 140%;
  margin-top: 5px;
`;

export default CommentValue;
